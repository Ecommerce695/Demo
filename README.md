# ecommerce

#### Web Project


## Install all Requirements which are in requirements.py file

pip install -r requirements.py

## To start Server run below command
#default port is 8000

python manage.py runserver
or
python manage.py runserver <port> 
